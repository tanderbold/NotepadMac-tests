[pytest]
testpaths = tests
timeout = 120
addopts = -ra
markers =
    slow: takes more than a few seconds
    network: needs a local test server
    restart: restarts the application
    case(id): the plan case a test implements
